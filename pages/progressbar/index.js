import React from 'react';
import RenderProgressBar from '../../components/progressbar'; // Adjust the import path as needed

const ProgressBar = () => {
  return (
    <div>
      <RenderProgressBar />
    </div>
  );
};

export default ProgressBar;