import React from 'react';
import Scheduler from '../../components/scheduler';

const SchedulerPage = () => {
  return (
    <div>
        <Scheduler/>
    </div>
  );
};

export default SchedulerPage;